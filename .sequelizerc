require("babel-register")
const { resolve } = require('path')

module.exports = {
  config: resolve(__dirname, "server/sequelize/config.js"),
  "models-path": resolve(__dirname, "server/models"),
  "migrations-path": resolve(__dirname, "server/sequelize/migrations"),
  "seeders-path": resolve(__dirname, "server/sequelize/seeds")
}
